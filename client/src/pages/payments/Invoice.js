import React from 'react';
import { Container, Typography } from '@mui/material';

const Invoice = () => {
  return (
    <Container>
      <Typography variant="h4">Invoice</Typography>
      <Typography>Invoice details will appear here.</Typography>
    </Container>
  );
};

export default Invoice;